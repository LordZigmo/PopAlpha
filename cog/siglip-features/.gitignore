# Cog-generated artifacts
.cog/
__pycache__/
*.pyc

# Local virtualenvs (if anyone runs the model outside Docker)
venv/
.venv/
env/

# CoreML build artifacts (regenerated by coreml_convert.py — 177MB
# FP16 .mlpackage; ship via ODR / iOS asset catalog, not git).
*.mlpackage/
*.mlmodelc/

# Editor / OS noise
.DS_Store
*.swp
