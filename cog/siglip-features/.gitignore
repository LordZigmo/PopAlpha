# Cog-generated artifacts
.cog/
__pycache__/
*.pyc

# Local virtualenvs (if anyone runs the model outside Docker)
venv/
.venv/
env/

# CoreML build artifacts (regenerated by coreml_convert.py — 177MB
# FP16 .mlpackage; ship via ODR / iOS asset catalog, not git).
*.mlpackage/
*.mlmodelc/

# Catalog bundle build artifact (regenerated by build_catalog_bundle.py
# — 35MB FP16 .papb; ship via Supabase Storage, downloaded on premium
# activation, not in git).
*.papb
*.papb.gz

# Editor / OS noise
.DS_Store
*.swp
